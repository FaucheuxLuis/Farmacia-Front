export class Login
{
    accessToken: string;    
    expiresIn: number;
    TokenType: string;
    RefreshToken: string;
    IdToken: string;
/*
    constructor(accessToken: string, expiresIn : number, TokenType: string, RefreshToken :string, IdToken : string)
    {
        this.accessToken = accessToken;
        this.expiresIn = expiresIn;
        this.TokenType = TokenType;
        this.RefreshToken = RefreshToken;
        this.IdToken = IdToken
    }
*/
}